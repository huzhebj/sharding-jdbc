<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sharding.mapper.StudentMapper">
    <insert id="addStudent" parameterType="com.sharding.pojo.Student" useGeneratedKeys="true" keyProperty="id">
        insert into tbl_student(
        name
        )VALUES(
        #{name}
        )
    </insert>

    <select id="getStudentList" parameterType="com.sharding.pojo.Student" resultType="com.sharding.pojo.Student">
        select id,name from tbl_student
        where 1 = 1
        <if test="id != null">
            and id = #{id}
        </if>
        <if test="name != null">
            and name like concat('%',#{name},'%')
        </if>
    </select>

    <update id="updateStudent" parameterType="com.sharding.pojo.Student">
        UPDATE tbl_student
        set name = #{name}
        where id = #{id}
    </update>

     <delete id="deleteStudent" parameterType="com.sharding.pojo.Student">
         delete from tbl_student
         where id = #{id}
     </delete>
</mapper>